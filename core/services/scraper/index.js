const cheerio = require('cheerio');
const rp = require('request-promise');
const config = require('./config');

class Scraper {
    constructor() {

    }
};

module.exports = {
    Scraper: Scraper
};
